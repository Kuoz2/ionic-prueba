import { BusquedappPipe } from './busquedapp.pipe';

describe('BusquedappPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedappPipe();
    expect(pipe).toBeTruthy();
  });
});
